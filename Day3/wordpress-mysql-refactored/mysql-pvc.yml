apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc-jegan       # Replace 'jegan' with your name
spec:
  selector:
    matchLabels:
      app: mysql
  accessModes:
  - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 2Gi
